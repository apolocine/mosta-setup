Le port du serveur NET de test est configurable via .env (MOSTA_NET_PORT).
Le port par défaut dans les scripts est 14488, mais l'utilisateur peut le changer.
Ne pas hardcoder le port — toujours lire depuis la config ou les arguments.
